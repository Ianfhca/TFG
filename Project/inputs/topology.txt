-- PARAMETERS --

# Time stages (m, s, ms, us, ns)
time 1 s
dt 200 ms #Integer value gratter than 0
>

-- HYPERPARAMETERS --

# Type 0 (Default)
v_reset -65.0
v_rest -65.0
v -65.0
v_th -50.0
lambda_v 20.0
t_refr 2
lambda_x 20.0
alpha 0.1
>

# Type 1
v_reset -60.0
v_rest -60.0
v -60.0
v_th -45.0
lambda_v 20.0
t_refr 1
lambda_x 20.0
alpha 0.1
>

-- TOPOLOGY --

# Capa 0 (entrada)
layer input
neurons 3
type 0 3 #(type amount)
connections dense
multisynapses 1
delay 100 250 #Same unit as dt
>

# Capa 1 (oculta 1)
layer excitatory
neurons 5
type 0 5
connections dense
multisynapses 1
delay 100 150
>

# Capa 2 (oculta 2)
layer excitatory
neurons 4
type 0 4
connections dense
multisynapses 2
delay 100 150
>

# Capa 3 (salida)
layer output
neurons 2
type 0 2
connections none
multisynapses 1
delay 100 150
>

-- INPUT --

# *There must be a "dt" time gap between each spike*

# Neuron 1 spikes
#para 100 N0 30 90 90 40
#para 100 N0 110 150 230 300
#para 500 N0 200
#N0 250 150 500
N0 250 155 500

# Neuron 2 spikes
#N1 50 180 270 100
#N1 200 350
#N1 250 150 500
N1 250 155 500

# Neuron 3 spikes
#N2 20 100 350 400
#N2 200 350
#N2 250 150 500
N2 250 155 500

# Falta realizar correctamente las sparse connections
# Eliminar las matrices de pesos etc. y cambiar por parametro en synapse

# Sparse connections de capa 0 a capa 1

# Hay que eliminar las spikes que se queden en una multisinapsis que no ha sido la encargada de que la post neurona llegue al umbral para el periodo de refraccion?
# Con la multisinapsis se envia la información por los multiples canales y se reciben todos en la neurona posterior con sus respectivos delays. ENTRA EN REFRACCION LA NEURONA ENTERA.

# El input de una neurona puede estar vacio?
# Si puede ser vacio pero pero no hacer ahora.

# Como gestiono que se generen multiples spikes en la capa de entrada por debajo de la cantidad de dt?

# V decae hasta cero pero realmente nunca es cero. 0,0000000000000000000000000008 NO TIENE SENTIDO YA

# Gestionar el maxDelay en el bucle. Que pasa si hay mas spikes que tiempo de simulación?

#connections sparse
#sparse_connection 0 0
#sparse_connection 1 1
#sparse_connection 2 1
#sparse_connection 3 0

# Tener en cuenta los delays acumulados para la finalización de la simulación

# Mirar delays para saber si se envian correctamente las spikes desde la capa 0

#lamdaX = -1/tau_m
#0.1 o 1 ms -> pasar a microsegundos

#codificación de datos para SNN

# Mirar esto si puede ser en el mismo ciclo de la spike: cycles = (dt == delay) ? 1 : (dt / delay) + 1;
# Con dos obtainSpike() funciona pero solo debería haber 1

# Comprobar que la forcing function no sea += en vez de = ·
# Arreglar delays ·
# TODO ------------------------------------------------------------------------------------- Modificar t del membrane potential

# Funcionamiento multisinapsis

# Corregir updateNeuron. Mirar si forcing function se tiene que hacer para multisyinapsis tambn. for(i; i < multisyinapsis; i++)

# Cambiar nombres tau etc. para los delays (Jose)

# Separar ficheros datos y estructura

# Enlazar dataset ·

# STDP (Backpropagation)

# Documentar cada método
# Gráficos