-- PARAMETERS --

# Time stages (m, s, ms, us, ns)
time 1 s
dt 100 ms # Integer value
>

-- HYPERPARAMETERS --

# Type 0 (Default)
v_reset -65.0
v_rest -65.0
v -65.0
v_th -50.0
lambda_v 20.0
t_refr 2.0
lambda_x 20.0
alpha 0.1
>

# Type 1
v_reset -60.0
v_rest -60.0
v -60.0
v_th -45.0
lambda_v 20.0
t_refr 1.5
lambda_x 20.0
alpha 0.1
>

-- TOPOLOGY --

# Capa 0 (entrada)
layer input
neurons 3
type 0 num_neurons 3
connections dense
multisynapses 1
delay 100 250 # Same unit as dt
>

# Capa 1 (oculta 1)
layer excitatory
neurons 5
type 0 num_neurons 5
connections dense
multisynapses 1
delay 100 150
>

# Capa 2 (oculta 2)
layer excitatory
neurons 4
type 0 num_neurons 4
connections dense
multisynapses 2
delay 100 150
>

# Capa 3 (salida)
layer output
neurons 2
type 1 num_neurons 2
connections none
multisynapses 1
delay 100 150
>

-- INPUT --

# Falta realizar correctamente las sparse connections
# Eliminar las matrices de pesos etc. y cambiar por parametro en synapse

# Sparse connections de capa 1 a capa 0 o de capa 0 a capa 1?
# Ver de que neurona tiene que tomar lambdaX, de la actual o de la anterior?
# Hay que eliminar las spikes que se queden en una multisinapsis que no ha sido la encargada de que la post neurona llegue al umbral para el periodo de refraccion?

#connections sparse
#sparse_connection 0 0
#sparse_connection 1 1
#sparse_connection 2 1
#sparse_connection 3 0